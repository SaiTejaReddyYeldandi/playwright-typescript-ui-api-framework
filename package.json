{
  "name": "playwright-typescript-ui-api-framework",
  "version": "1.0.0",
  "description": "Playwright + TypeScript framework testing the same app two ways - UI (Page Object Model, cross-browser) and REST API - in CI against a Dockerised Node app.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "test:chromium": "playwright test --project=chromium",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
